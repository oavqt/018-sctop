import { FC } from 'react';
import StyledApp from './styled/App.styled';

const App: FC = () => {
  return (
    <StyledApp>
      <div>{"le vt'que"}</div>
    </StyledApp>
  );
};

export default App;
