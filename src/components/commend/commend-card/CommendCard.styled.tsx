import styled from 'styled-components';

const StyledCommendCard = styled.article`
  display: flex;
  flex-direction: column;
  height: 25em;
  justify-content: center;
  position: relative;
  width: 25em;
`;

export default StyledCommendCard;
